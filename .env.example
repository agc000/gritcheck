# Public Supabase config — safe in the browser bundle (RLS guards the data, §3.5).
# Copy to .env.local and fill in the anon key from Supabase → Settings → API.
NEXT_PUBLIC_SUPABASE_URL=https://idxgxqxgvxypanzxtusc.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Server-only. NEVER prefix with NEXT_PUBLIC_ and never commit a real value.
# Used by the seed script and (later) the scraper to write with RLS bypassed.
SUPABASE_SERVICE_ROLE_KEY=
